# Testes SQL

Testes somente leitura para validar cada origem sem `JOIN`, comparar filtros e contagens, adicionar junções incrementalmente e reconciliar o resultado final.

Use o mesmo nome-base do artefato testado com o sufixo `_test.sql`.

